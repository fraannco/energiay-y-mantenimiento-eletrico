import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#d32f2f',
        },
        secondary: {
            main: '#ff9100',
        },
    },
});

export default theme;